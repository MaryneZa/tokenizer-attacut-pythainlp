from typing import Dict, Union

# Define the static model configuration as a constant dictionary
STATIC_MODEL_CONFIG: Dict[str, Union[int, float]] = {
    "embc": 32,
    "embs": 16,
    "conv": 64,
    "l1": 32,
    "do": 0.0,
}